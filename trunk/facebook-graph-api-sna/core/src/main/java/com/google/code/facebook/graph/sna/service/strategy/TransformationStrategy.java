package com.google.code.facebook.graph.sna.service.strategy;

/**
 * The Enum TransformationStrategy.
 */
public enum TransformationStrategy implements StrategyEnum {
	
	/** The DIRECTIO n_ transformer. */
	DIRECTION_TRANSFORMER,
	
	/** The FOLDIN g_ transformer. */
	FOLDING_TRANSFORMER,
	
	/** The VERTE x_ partitio n_ collapser. */
	VERTEX_PARTITION_COLLAPSER;
}
